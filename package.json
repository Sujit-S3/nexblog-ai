{
  "name": "mern-blog",
  "version": "3.2.7",
  "description": "Enterprise AI Knowledge & Content Operating System (monorepo orchestrator). Real dependencies live in api/package.json and client/package.json — this file intentionally declares none of its own to avoid the two copies drifting out of sync.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix api",
    "start": "npm start --prefix api",
    "build": "npm ci --prefix api && npm ci --prefix client && npm run build --prefix client",
    "test": "npm test --prefix api",
    "lint": "npm run lint --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
